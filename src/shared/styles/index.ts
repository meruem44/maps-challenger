import { SF_FONTS } from "./fonts/sf-fonts";
import { METRICS } from "./metrics/metrics";
import { LIGHT_THEME } from "./themes/light.theme";

export const THEME = {
  COLORS: LIGHT_THEME,
  METRICS,
  FONTS: SF_FONTS,
};
